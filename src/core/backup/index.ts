// backup — portable snapshots of the user's data for backup and restore
export { buildBackup, BACKUP_VERSION } from './buildBackup';
export { parseBackup } from './parseBackup';
export type { ParseResult } from './parseBackup';
