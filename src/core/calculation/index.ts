// calculation — pure salary math: resolve rules, allocate buckets, per-day rate
export { resolveRuleAmount } from './resolveRuleAmount';
export { allocateBuckets } from './allocateBuckets';
export { perDay } from './perDay';
