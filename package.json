{
  "name": "allocato",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Salary planner: allocate a salary across buckets; whatever is left over is the spendable daily allowance.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
